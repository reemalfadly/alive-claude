# ── الأساس (كل الحواس تحتاجها) ──
requests>=2.31
numpy>=1.24
Pillow>=10.0
mss>=9.0
psutil>=5.9
python-dotenv>=1.0

# ── الصوت — ويندوز ──
sounddevice>=0.4 ; sys_platform == "win32"
soundcard>=0.4   ; sys_platform == "win32"

# ── اختياري: تفريغ الصوت محلياً (تحميل ~١ قيقا أول مره) ──
# faster-whisper>=1.0
